1:开始---->运行---->cmd，或者是window+R组合键，调出命令窗口。

2:输入命令：netstat -ano，列出所有端口的情况。在列表中我们观察被占用的端口，比如是80，首先找到它,可以看到它的PID为7492。
  或者 查看被占用端口对应的PID，输入命令：netstat -aon|findstr "80"，回车，记下最后一位数字，即PID,这里是7492.

3:继续输入tasklist|findstr 7492，回车，查看是哪个进程或者程序占用了2720端口，结果是：nginx.exe。

4:打开任务管理器后将nginx.exe 结束即可。